# Enter your code here. Read input from STDIN. Print output to STDOUT

# This code capitalizes only first letter of each word

inputString = raw_input().split()                               # Splits string into substrings

for i in range(0,len(inputString)):                             # capitalizes each word individually
    currString = inputString[i]
    currString = currString[:1].upper() + currString[1:]
    inputString[i] = currString
    
print " ".join(inputString)                                     # rejoins list with space " " in between and prints 
