# This code counts the number of occurences of sub-strings in main string

# Enter your code here. Read input from STDIN. Print output to STDOUT

inputString, subString = raw_input(), raw_input()
count = 0

for i in range(0,len(inputString)):
    if inputString[i:i+len(subString)].find(subString) >= 0:
        count = count + 1

print count
