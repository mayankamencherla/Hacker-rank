# Enter your code here. Read input from STDIN. Print output to STDOUT
N = int(raw_input())

a = sorted(list(map(int,raw_input().split())))

max_a = max(a)

for i in range(a.count(max_a)):
    a.remove(max_a)
    
print max(a)
